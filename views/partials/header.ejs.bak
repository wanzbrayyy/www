<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PLASMADINAH | Essential Oil Manufacturer</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-black: #000000;
            --secondary-black: #111111;
            --primary-gold: #bf9b30;
            --light-gold: #d4ac3d;
            --text-white: #ffffff;
            --text-grey: #cccccc;
            --whatsapp-green: #25D366;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--primary-black);
            color: var(--text-white);
            overflow-x: hidden;
            line-height: 1.5;
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }
        ul { list-style: none; }
        img { width: 100%; display: block; }

        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        .text-center { text-align: center; }
        .text-gold { color: var(--primary-gold); }
        .bg-gold { background-color: var(--primary-gold); color: var(--primary-black); }
        
        .section-title {
            font-size: 2rem;
            font-weight: 800;
            text-transform: uppercase;
            margin-bottom: 40px;
            letter-spacing: 1px;
            text-align: center;
        }

        .btn-gold {
            background-color: var(--primary-gold);
            color: var(--primary-black);
            padding: 12px 30px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-radius: 50px;
            display: inline-block;
            border: none;
            cursor: pointer;
        }
        .btn-gold:hover { background-color: var(--light-gold); color: #000; }

        .navbar {
            padding: 10px 40px;
            background-color: var(--primary-black);
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid #222;
        }

        .nav-logo-container {
            flex-shrink: 0;
        }
        
        .nav-logo-img {
            height: 80px;
            width: auto;
            max-width: 250px;
            display: inline-block;
        }

        .nav-links {
            display: flex;
            gap: 30px;
            font-size: 0.8rem;
            font-weight: 600;
            color: var(--primary-gold);
            text-transform: uppercase;
        }

        .nav-left, .nav-right {
            flex: 1;
            display: flex;
            align-items: center;
        }
        .nav-left { justify-content: flex-start; }
        .nav-right { justify-content: flex-end; }

        .mobile-toggle { display: none; color: var(--primary-gold); font-size: 1.8rem; cursor: pointer; z-index: 1003; }
        
        .hero-section {
            display: flex;
            width: 100%;
            gap: 2px;
            background: #222;
        }

        .hero-card {
            position: relative;
            flex: 1;
            height: 65vh;
            overflow: hidden;
            background: #000;
        }

        .hero-card img {
            height: 100%;
            width: 100%;
            transition: transform 0.6s ease, filter 0.6s ease;
            filter: brightness(0.6);
            object-fit: cover;
        }

        .hero-card:hover img { transform: scale(1.05); filter: brightness(0.9); }

        .hero-content {
            position: absolute;
            bottom: 10%;
            left: 50%;
            transform: translateX(-50%);
            width: 100%;
            text-align: center;
            padding: 0 20px;
            z-index: 2;
        }

        .hero-title {
            font-size: 1.8rem;
            font-weight: 300;
            text-transform: uppercase;
            letter-spacing: 1px;
            line-height: 1.3;
            color: var(--text-white);
            margin-bottom: 25px;
            white-space: pre-line;
        }

        .about-section { padding: 80px 20px; text-align: center; }
        .about-title { font-size: 2.5rem; font-weight: 800; text-transform: uppercase; margin-bottom: 30px; color: var(--primary-black); }
        .about-text { font-size: 1.1rem; max-width: 800px; margin: 0 auto 15px; color: #333; }
        .about-subtext { font-size: 0.9rem; margin-top: 20px; font-weight: 600; }

        .services-section { background: #f4f4f4; color: #333; padding: 80px 0; }
        .service-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; }
        .service-card { background: #fff; padding-bottom: 20px; box-shadow: 0 5px 15px rgba(0,0,0,0.05); }
        .service-img { height: 250px; width: 100%; object-fit: cover; margin-bottom: 15px; }
        .service-info { padding: 0 20px; }
        .service-title { font-weight: 700; font-size: 1.1rem; margin-bottom: 10px; color: #000; }
        .service-desc { font-size: 0.85rem; color: #666; line-height: 1.6; }

        .category-section { background-color: var(--primary-black); padding: 100px 20px; text-align: center; }
        .cat-title { color: var(--primary-gold); font-size: 2rem; font-weight: 700; text-transform: uppercase; margin-bottom: 20px; }
        .cat-sub { color: #fff; font-size: 1.2rem; font-weight: 300; margin-bottom: 40px; }

        .blog-section { background: #f9f9f9; color: #333; padding: 80px 0; }
        .blog-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; }
        .blog-card { background: white; box-shadow: 0 5px 15px rgba(0,0,0,0.05); transition: 0.3s; }
        .blog-card:hover { transform: translateY(-5px); }
        .blog-img { height: 220px; width: 100%; object-fit: cover; }
        .blog-content { padding: 25px; }
        .blog-meta { font-size: 0.7rem; color: #999; text-transform: uppercase; margin-bottom: 10px; font-weight: 700; }
        .blog-title { font-size: 1.1rem; font-weight: 700; line-height: 1.4; margin-bottom: 15px; color: #222; height: 50px; overflow: hidden; }
        .blog-snippet { font-size: 0.85rem; color: #666; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden; }

        .market-section { background: #111; padding: 60px 0; text-align: center; }
        .market-img { max-width: 800px; margin: 0 auto; filter: invert(1); opacity: 0.8; }
        
        .impact-section { background: var(--primary-gold); padding: 60px 0; }
        .impact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; }
        .impact-card { position: relative; height: 400px; border-radius: 8px; overflow: hidden; }
        .impact-text { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); font-size: 2rem; font-weight: 800; color: #000; text-align: center; text-shadow: 0 0 10px rgba(255,255,255,0.5); }

        .detail-header { text-align: center; padding: 60px 20px; background: #111; color: var(--primary-gold); }
        .detail-container { max-width: 900px; margin: -50px auto 50px; background: #fff; padding: 40px; color: #333; border-radius: 8px; position: relative; }
        .detail-img { width: 100%; height: 400px; object-fit: cover; border-radius: 4px; margin-bottom: 30px; }
        .comment-box { background: #f4f4f4; padding: 30px; margin-top: 40px; border-radius: 8px; }
        .form-control { width: 100%; padding: 12px; margin-bottom: 15px; border: 1px solid #ddd; background: #fff; border-radius: 4px; }
        
        .whatsapp-float { position: fixed; bottom: 25px; right: 25px; background-color: var(--whatsapp-green); color: white; padding: 12px 20px; border-radius: 50px; display: flex; align-items: center; gap: 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.2); z-index: 1002; font-size: 1rem; font-weight: 600; }
        .whatsapp-float:hover { transform: scale(1.05); }
        .whatsapp-float i { font-size: 1.5rem; }

        .modal-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.9); z-index: 1001;
            display: none; justify-content: center; align-items: center;
            backdrop-filter: blur(5px);
        }
        .modal-content {
            background: #111; color: #fff;
            padding: 40px; border-radius: 8px;
            width: 90%; max-width: 900px;
            max-height: 80vh; overflow-y: auto;
            position: relative; border: 1px solid var(--primary-gold);
            box-shadow: 0 0 30px rgba(191, 155, 48, 0.3);
        }
        .modal-close {
            position: absolute; top: 15px; right: 20px;
            font-size: 2rem; color: #fff; cursor: pointer;
            line-height: 1;
        }
        .modal-close:hover { color: var(--primary-gold); }
        .product-list {
            list-style: none; column-count: 3; column-gap: 30px;
        }
        .product-list li {
            padding: 8px 0; border-bottom: 1px solid #333; color: var(--text-grey);
            page-break-inside: avoid; break-inside: avoid;
        }

        @media (max-width: 992px) {
            .navbar { justify-content: space-between; padding: 10px 20px; flex-wrap: wrap; }
            .nav-logo-container { flex: 1; text-align: left; }
            .nav-logo-img { height: 60px; }
            .nav-left, .nav-right {
                display: none;
                flex-direction: column;
                position: absolute;
                background: #000;
                width: 100%;
                left: 0;
                top: 80px;
                padding: 10px 0;
                gap: 25px;
                text-align: center;
                border-bottom: 1px solid #222;
            }
            .nav-left.active { display: flex; }
            .nav-right.active { display: flex; top: 220px; }
            .mobile-toggle { display: block; }

            .hero-section { 
                display: flex;
                overflow-x: auto;
                scroll-snap-type: x mandatory;
                padding: 10px;
                gap: 10px;
                -ms-overflow-style: none;
                scrollbar-width: none;
            }
            .hero-section::-webkit-scrollbar { display: none; }
            .hero-card { 
                min-width: 85%;
                height: 50vh;
                scroll-snap-align: center;
                border-radius: 8px;
            }
            
            .service-grid { grid-template-columns: repeat(2, 1fr); }
            .blog-grid, .impact-grid { grid-template-columns: 1fr; }
            .about-title { font-size: 1.8rem; }
            .impact-card { height: 250px; }
            .product-list { column-count: 2; }
        }
        @media (max-width: 576px) {
            .service-grid { grid-template-columns: 1fr; }
            .product-list { column-count: 1; }
            .whatsapp-float span { display: none; }
            .whatsapp-float { padding: 15px; }
            .whatsapp-float i { font-size: 1.2rem; margin: 0; }
        }
    </style>
</head>
<body>

<nav class="navbar">
    <div class="nav-left" id="navLeft">
        <a href="/" class="nav-links">HOME</a>
        <a href="#products" class="nav-links">PRODUCTS</a>
        <a href="#b2b" class="nav-links">B2B</a>
    </div>

    <div class="nav-logo-container">
        <a href="/">
            <img src="https://files.catbox.moe/opos2r.jpg" alt="Plasmadinah Logo" class="nav-logo-img">
        </a>
    </div>

    <div class="nav-right" id="navRight">
        <a href="#blog" class="nav-links">BLOG</a>
        <a href="#footer" class="nav-links">CONTACT</a>
        <% if(user) { %>
            <a href="/admin" class="nav-links text-gold">ADMIN</a>
        <% } %>
    </div>

    <div class="mobile-toggle" onclick="toggleMenu()">
        <i class="fas fa-bars"></i>
    </div>
</nav>